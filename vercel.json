{
  "framework": "nestjs",
  "installCommand": "npm install",
  "buildCommand": "nest build",
  "builds": [
    {
      "src": "./dist/main.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/" }]
}
